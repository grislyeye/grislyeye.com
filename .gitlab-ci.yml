image: node:7

cache:
  paths:
  - node_modules/

before_script:
  - npm install
  - npm install -g bower
  - bower install --allow-root

pages:
  script:
  - npm run build
  - mv dist public
  artifacts:
    paths:
    - public
  only:
  - tags

lint:
  script:
  - npm run build
