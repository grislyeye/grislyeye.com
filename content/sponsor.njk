---
title: The Grisly Eye - Sponser Me
layout: layouts/base.njk
---
<header>
  <my-hero subtitle="Subscribe">
    <span slot="title">Sponsor</span>
    <span slot="description">
      If you like my <em>pro bono</em> work, you could buy one of my books to show your appreciation. It'll make my day.
    </span>
  </my-hero>
</header>

<my-section>
  <div slot="subtitle">buy</div>
  <ul id="products" class="previews">
    {% for post in collections.published | excludeTags([ "drafts" ]) | reverse %}
      <li class="preview">
        {% if (post.data.redirectTo) %}{% set url %}{{ post.data.redirectTo }}{% endset %}
        {% else %}{% set url %}{{ post.url }}{% endset %}
        {% endif %}

        <a href="{{ url }}" title="{{ post.data.title }}">
          <my-preview
            class="{{ post.data.tags.join(' ') }} {{ theme }} featured"
            {% if post.data.image %}
              {% set imagePath %}.{{ post.inputPath }}/{{ post.data.image }}{% endset %}
              background="{% rawImageUrl post, post.data.image, 300 %}"
            {% endif %}
            preload
          >
            <span slot="title">{{ post.data.title }}</span>
          </my-preview>
        </a>
      </li>
    {% endfor %}
  </ul>
</my-section>
