---
layout: layouts/home.njk
eleventyNavigation:
  key: Home
  order: 1
---
<header>
  <my-hero>
    <span slot="title">The <br> Grisly Eye</span>
    <span slot="subtitle">{{ metadata.description }} You can buy my stuff below, or follow/contact me via my socials above.</span>
  </my-hero>
</header>

<ul id="more" class="previews">
  {% for post in collections.products | reverse %}
    <li>
      <a href="{{ post.url }}">

        <my-preview
          types="{{ post.data.tags.join(' ') }}"
        >
          <span slot="title">{{ post.data.title }}</span>
          {% if post.data.image %}
            {% set imagePath %}.{{ post.url }}{{ post.data.image }}{% endset %}
            <span slot="background">
              {% image imagePath, post.data.title, [ "165px" ] %}
            </span>
          {% endif %}
        </my-preview>

      </a>
    </li>
	{% endfor %}
</ul>
